'use strict';

(function() {
    var module = angular.module('recast', []);
    module.factory('Recast', ['$window', function($window) {

        // if (Meteor.isServer) return null;

        // return $window.Recast;

        return null;
    }]);
})();
